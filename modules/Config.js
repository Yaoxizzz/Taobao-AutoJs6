/**
 * @description 全局配置
 */
var Config = {
    packageName: "com.taobao.taobao",
    
    // 任务开关
    ENABLE_SIGN: true,
    ENABLE_TASK: false,

    // 防检测刷新 (针对88VIP等)
    enableLayoutRefresh: true,
    
    debug: true
};
module.exports = Config;
